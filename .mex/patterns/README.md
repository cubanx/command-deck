# Patterns

Add a pattern only when a project-specific workflow recurs or a non-obvious failure needs a durable runbook. Prefer existing context over duplication.

Each pattern must name when to use it, the minimum steps, known gotchas, verification, and links to relevant context. Add it to `INDEX.md`.
